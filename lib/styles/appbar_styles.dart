import 'package:flutter/material.dart';

class AppbarStyles {
  static const Color corFundo = Colors.deepPurple;
  static const Color corIcone = Colors.white;
  static const TextStyle textoTitulo = TextStyle(
    color: Colors.white,
    fontWeight: FontWeight.bold,
  );
}